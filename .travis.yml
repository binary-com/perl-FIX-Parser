language: perl
matrix:
    fast_finish: true
services:
    - redis-server
perl:
    - "5.14"
before_install:
    - cpanm --installdeps .
    - perl -V
script:
    perl Makefile.PL && PERL5OPT=-MDevel::Cover make test
after_success:
  - bash <(curl -s https://codecov.io/bash)

